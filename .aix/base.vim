"  --------------  Aix Vim --------------
"  VERSION 1.0


" Not complete with Vi Mode
set nocompatible

set regexpengine=1

" History
set history=9888

if !exists($HOME)
  let $HOME=expand('~')
endif

